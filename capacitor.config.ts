import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ExamenU3EquipoOverTime',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
